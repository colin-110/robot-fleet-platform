# Robot Fleet Dashboard (Frontend)

## Run (Windows PowerShell)

PowerShell may block `npm.ps1` depending on your execution policy, so use `npm.cmd`.

```powershell
cd frontend\robot-fleet-dashboard
npm.cmd install
npm.cmd run dev -- --host
```

Or from `frontend/`:

```powershell
cd frontend
npm.cmd run dev
```
