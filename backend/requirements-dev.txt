# Test-only. Not installed in the runtime image — the Dockerfile builds
# from requirements.txt alone, since none of this is imported by app/.
-r requirements.txt

pytest>=8,<9
pytest-asyncio>=0.23
pytest-cov>=5,<7
httpx>=0.27,<1
